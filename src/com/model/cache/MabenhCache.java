/*
 * Very basic JavaBean cache
 * Created on 17 Jul 2017 ( Date ISO 2017-07-17 - Time 08:25:19 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */

package com.model.cache;

import java.util.Hashtable;
import java.util.ArrayList;
import java.util.Map;

import com.model.dao.Mabenh ;

/**
 * Very basic cache for Mabenh instances (just for the Telosys Tools demo)
 * 
 * @author Telosys Tools Generator
 *
 */
public class MabenhCache
{
	private final static Map<String,Mabenh> cache = new Hashtable<String,Mabenh>() ;
	public final static ArrayList<String> cacheArrayListKey = new ArrayList<String>() ;
	
	/**
	 * Build the cache key from the Primary Key field(s)
     * @param mabenhId MABENH_ID 
	 * @return the key
	 */
	private final static String getKey( String mabenhId ) {
		return ""  + mabenhId  ;
	}

	/**
	 * Put the given Mabenh instance in the cache
	 * @param Mabenh instance to be stored
	 */
	public final static void putMabenh(Mabenh mabenh ) {
		String key = getKey( mabenh.getMABENH_ID());
		cache.put(key, mabenh );
	}
	
	/**
	 * Get the Mabenh instance for the given primary key
     * @param mabenhId 
	 * @return the Mabenh instance (or null if none)
	 */
	public final static Mabenh getMabenh( String mabenhId ) {
		String key = getKey( mabenhId ) ;
		return cache.get(key);
	}

	/**
	 * Removes the Mabenh associated with the given primary key
     * @param mabenhId 
	 */
	public final static void removeMabenh ( String mabenhId ) {
		String key = getKey( mabenhId ) ;
		cache.remove(key);
	}
	
	/**
	 * Removes the given Mabenh from the cache using its primary key
	 * @param Mabenh instance to be removed
	 */
	public final static void removeMabenh (Mabenh mabenh ) { 
		String key = getKey( mabenh.getMABENH_ID());
		cache.remove(key);
	}

}
